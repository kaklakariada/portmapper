<project name="PortMapper" basedir="." default="main">

	
	<property name="version.number" value="1.1"/>
	
	<property name="src.dir" value="src" />
	<property name="build.dir" value="build" />
	<property name="classes.dir" value="${build.dir}/classes" />
	<property name="jar.dir" value="${build.dir}/jar" />

	<property name="main-class" value="org.chris.portmapper.PortMapperApp" />
	
	<buildnumber />
	<property name="version" value="${version.number}_build${build.number}" />
	<property name="lib.dir" value="lib" />

	<property name="one-jar.version" value="0.96" />
	<taskdef name="one-jar" classname="com.simontuffs.onejar.ant.OneJarTask" classpath="${lib.dir}/one-jar-ant-task-${one-jar.version}.jar" onerror="report" />


	<path id="classpath">
		<fileset dir="${lib.dir}" includes="**/*.jar" />
	</path>

	<target name="clean">
		<delete dir="${build.dir}" />
	</target>

	<target name="compile">
		<mkdir dir="${classes.dir}" />
		<javac srcdir="${src.dir}" destdir="${classes.dir}" classpathref="classpath" />
		<copy todir="${classes.dir}">
			<fileset dir="${src.dir}" excludes="**/*.java" />
		</copy>
		<move file="${classes.dir}/org/chris/portmapper/resources/PortMapperApp_en.properties" tofile="${classes.dir}/org/chris/portmapper/resources/PortMapperApp.properties" />
		<move file="${classes.dir}/org/chris/portmapper/resources/ConnectTask_en.properties" tofile="${classes.dir}/org/chris/portmapper/resources/ConnectTask.properties" />
	</target>

	<target name="jar" depends="compile">
		<mkdir dir="${jar.dir}" />
		<jar destfile="${jar.dir}/${ant.project.name}.jar" basedir="${classes.dir}">
			<manifest>
				<attribute name="Main-Class" value="${main-class}" />
			</manifest>
		</jar>
	</target>

	<target name="one-jar" depends="jar">
		<!-- Construct the One-JAR file -->
		<one-jar destfile="${jar.dir}/${ant.project.name}-${version}.jar" manifest="one-jar.mf">
			<main>
				<!-- Construct main.jar from classes and source code -->
				<fileset dir="${classes.dir}" />
			</main>
			<lib>
				<fileset file="${lib.dir}/*.jar" />
			</lib>
		</one-jar>
	</target>

	<target name="run" depends="jar">
		<java fork="true" classname="${main-class}">
			<classpath>
				<path refid="classpath" />
				<path location="${jar.dir}/${ant.project.name}.jar" />
			</classpath>
		</java>
	</target>

	<target name="clean-build" depends="clean,jar" />

	<target name="main" depends="clean,run" />

</project>